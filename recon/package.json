{
  "name": "mailrecon",
  "version": "0.1.0",
  "description": "Email-auth + subdomain-takeover recon for bug bounty and security assessments. Passive by default, active gated behind scope authorization.",
  "type": "module",
  "bin": {
    "mailrecon": "./src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "typecheck": "bun --bun tsc --noEmit"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/bun": "latest"
  }
}
